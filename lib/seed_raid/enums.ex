import EctoEnum

defenum(RaidTypeEnum, :raidtype, [
  :starlight_rose,
  :mix,
  :foxflower,
  :dreamleaf,
  :fjarnskaggl,
  :aethril
])

defenum(RegionEnum, :region, [:eu, :us])
defenum(SideEnum, :side, [:horde, :alliance])
